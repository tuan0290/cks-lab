# Solution: OPA Gatekeeper Policy Enforcement

Apply all manifests from Steps 2-5 of the README. Requires Gatekeeper to be installed.

```bash
# Check if Gatekeeper is installed
kubectl get pods -n gatekeeper-system
```
